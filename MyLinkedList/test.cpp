#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;
/*
bool cmp( int elem1, int elem2 )
{
	return elem1 > elem2;
}

int solution(vector<int> &vec)
{
	sort(vec.begin(), vec.end(), cmp);
	return 0;
}

void main(void)
{
	vector<int> vec;
	vec.push_back(100);
	vec.push_back(1);
	vec.push_back(35);
	vec.push_back(3);
	vec.push_back(5);
	solution(vec);
}
*/